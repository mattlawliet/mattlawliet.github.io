---
id: T09
title: Breaking the frame for the non-Minecraft three
labels: [wayfinder:prototype]
map: talkable-portfolio
status: open
assignee:
blocked-by: [T01, T05]
---

## Question

`Parakon` (push-to-talk transcription), `speak` (a 0.6B voice-cloning TTS at
0.7x realtime on a 4060) and `Models & Textures` do not belong in a chest slot.
Charting decided the seam is the joke: the last tier visibly breaks the
inventory frame rather than pretending to fit.

- How far does it break — different card shape, different type, no slots at
  all, a deliberate change of medium?
- Does the tooltip mechanic survive the tier, or stop at the boundary?
- `Models & Textures` is the one entry with a genuine 3D viewer and three
  models. It may deserve its own treatment rather than sharing a tier with two
  Python tools.
- The joke has to land as intentional in under a second, or it reads as a bug.
