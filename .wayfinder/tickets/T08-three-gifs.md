---
id: T08
title: Three GIFs worth recording
labels: [wayfinder:task]
map: talkable-portfolio
status: open
assignee:
blocked-by: [T02, T03]
---

## Question

Manual capture the agent cannot do. `wtf`, `Shopah` and `DeathChestReborn` earn
motion; the rest live on rendered visuals and type.

Blocked until the page layout fixes the frame, so nothing is recorded twice.
Resolving this ticket means handing the user an exact checklist — per clip: the
action to perform, aspect ratio, pixel dimensions, duration ceiling, file size
ceiling, format (GIF vs looping muted MP4 vs WebP), and where the file lands in
the repo — then the files existing.

Suggested subjects, to confirm: `wtf`'s searchable grid finding an item across
tracked containers; `Shopah`'s sign-on-a-barrel becoming a working shop; a
`DeathChestReborn` death and retrieval. Each should read at a glance with no
audio and no caption.
